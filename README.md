# semantic-rag-engine

## What is RAG?
	- RAG is Retreival Augmentation Generation, a process in which the LLM model can generate responses from given set of documents without having to fine-tune the entire model. It uses the concept of semantic similarity to identify the correct answers from the given set of data.

## The 3-step process (Retreival, Augmentation, Generation)
	- Retreival - in this process, the user 
## The key concepts:
	- What are embeddings?
	- What's a vector database?
	- Why semantic search > keyword search?
## Why RAG matters (real use cases)
## Common mistakes to avoid
